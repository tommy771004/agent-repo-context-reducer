# Read admission

Whole-file reads are expensive. Prefer structure or symbols for large files unless the task truly requires full source. Secret-like, generated, binary, symlink and oversized paths are guarded by default.
