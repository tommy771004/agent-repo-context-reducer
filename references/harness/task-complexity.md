# Task complexity

Small/focused tasks should remain single-agent unless risk or ambiguity requires escalation. Cross-module integration, migrations and broad refactors may justify multi-agent schedules. Classification is heuristic, not a correctness guarantee.
