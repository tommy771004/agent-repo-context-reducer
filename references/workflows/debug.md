# Debug workflow

Search task/error terms, identify relevant routes/services, follow static dependency neighborhood, read bounded symbols, preserve hypotheses/evidence separately and widen context only when unresolved evidence remains.
