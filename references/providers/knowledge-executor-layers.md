# Knowledge and executor layers

`repository.graph` is static code structure. `knowledge.search` is project docs/history retrieval; native fallback is lexical and not GraphRAG. `knowledge.graph`, `executor.code` and `executor.autonomous` require external providers when no compatible native implementation exists.
