{
  "name": "agent-repo-context-reducer-skill",
  "version": "0.1.0",
  "private": true,
  "description": "Agent Skill that builds compact repository context before an AI agent reads a codebase.",
  "scripts": {
    "test": "python -m unittest discover -s tests -v",
    "demo": "python scripts/repo_context.py scan examples/sample-project --pretty"
  }
}
