# Model routing

The planner uses vendor-neutral tiers `cheap`, `standard` and `strong`. Deterministic sort/routing is preferred. Risk, ambiguity, novelty and cost of error may escalate planner/worker/grader tiers. A tier is advisory until a compatible `model.*` provider exists.
