# Routing

Routing is deterministic-first. Intent, complexity, risk, ambiguity and novelty choose workflow and advisory model tiers. Concrete vendor models are resolved only when compatible `model.*` providers exist.
