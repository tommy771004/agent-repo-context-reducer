# Agreement and Provenance

`occurrence_count` counts retained input occurrences. `agreement_count` counts unique workers. `independent_source_count` counts unique source locations. `independent_evidence_count` counts source/evidence/provenance identities. Duplicate payload can be removed, but worker/source/provenance support is aggregated. A repeated finding by one worker does not become multi-worker agreement.
