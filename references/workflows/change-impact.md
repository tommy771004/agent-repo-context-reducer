# Change-impact workflow

Start from changed/target files, follow static reverse dependencies and affected neighborhoods, inspect public contracts/tests, and report static-analysis limits explicitly.
